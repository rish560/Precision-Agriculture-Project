import { CalendarDays, KeyRound, Mail, MapPin, Pencil, Phone, ShieldCheck, UserCircle, X } from 'lucide-react';
import { useState } from 'react';
import { Button } from '../components/ui/Button';
import { Card } from '../components/ui/Card';
import { useAuth } from '../context/AuthContext';
import { useToast } from '../context/ToastContext';

export const ProfilePage = () => {
  const { user, updateProfile } = useAuth(); const { addToast } = useToast();
  const profile = user || { fullName: 'Guest User', email: 'guest@farmverse.com', role: 'Guest', phone: '', address: '', joinedAt: '14 July 2026' };
  const [dialog, setDialog] = useState(null); const [draft, setDraft] = useState(profile);
  const details = [[UserCircle, 'Full Name', profile.fullName], [Mail, 'Email', profile.email], [ShieldCheck, 'Role', profile.role], [Phone, 'Phone Number', profile.phone || 'Not provided'], [MapPin, 'Address', profile.address || 'Not provided'], [CalendarDays, 'Join Date', profile.joinedAt || 'Account active']];
  const save = (event) => { event.preventDefault(); updateProfile(draft); setDialog(null); addToast(dialog === 'password' ? 'Password updated successfully.' : 'Profile updated successfully.', 'success'); };
  return <div className="space-y-6"><Card className="overflow-hidden p-0"><div className="relative h-44 overflow-hidden"><img src="https://images.unsplash.com/photo-1501004318641-b39e6451bec6?auto=format&fit=crop&w=1400&q=80" alt="Agriculture field and irrigation system" className="h-full w-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-slate-900/70 via-slate-900/20 to-transparent" /></div><div className="relative px-6 pb-6 pt-14 sm:px-8"><div className="absolute -top-11 left-6 flex h-22 w-22 items-center justify-center rounded-3xl border-4 border-white bg-emerald-100 text-emerald-700 shadow-lg sm:left-8"><UserCircle className="h-16 w-16" /></div><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-emerald-700">User profile</p><p className="mt-2 text-2xl font-semibold text-slate-900">{profile.fullName}</p><p className="mt-1 text-sm text-slate-500">Account details and security settings.</p></div><div className="flex flex-wrap gap-2"><Button onClick={() => { setDraft(profile); setDialog('profile'); }} className="px-4 py-2 text-sm"><Pencil className="mr-2 inline h-4 w-4" />Edit Profile</Button><Button variant="secondary" onClick={() => setDialog('password')} className="px-4 py-2 text-sm"><KeyRound className="mr-2 inline h-4 w-4" />Change Password</Button></div></div><div className="mt-6 grid gap-3 text-sm text-slate-700 sm:grid-cols-2 lg:grid-cols-3">{details.map(([Icon, label, value]) => <div key={label} className="rounded-2xl border border-slate-100 bg-slate-50 p-4"><Icon className="h-4 w-4 text-emerald-700" /><p className="mt-3 text-xs font-semibold uppercase tracking-[0.16em] text-slate-400">{label}</p><p className="mt-1 font-medium text-slate-900">{value}</p></div>)}</div></div></Card>{dialog && <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/40 p-4"><Card className="w-full max-w-lg" hover={false}><div className="flex justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.2em] text-emerald-700">Account settings</p><h2 className="mt-2 text-xl font-semibold">{dialog === 'profile' ? 'Edit Profile' : 'Change Password'}</h2></div><button type="button" aria-label="Close dialog" onClick={() => setDialog(null)} className="rounded-full border p-2"><X className="h-4 w-4" /></button></div><form onSubmit={save} className="mt-6 space-y-4">{dialog === 'profile' ? <>{[['fullName', 'Full name'], ['phone', 'Phone number'], ['address', 'Address']].map(([key, label]) => <label key={key} className="block text-sm font-medium">{label}<input required value={draft[key] || ''} onChange={(event) => setDraft((current) => ({ ...current, [key]: event.target.value }))} className="mt-2 w-full rounded-2xl border bg-slate-50 px-4 py-3 outline-none focus:border-emerald-400" /></label>)}</> : <><label className="block text-sm font-medium">Current password<input required type="password" className="mt-2 w-full rounded-2xl border bg-slate-50 px-4 py-3 outline-none focus:border-emerald-400" /></label><label className="block text-sm font-medium">New password<input required minLength="8" type="password" className="mt-2 w-full rounded-2xl border bg-slate-50 px-4 py-3 outline-none focus:border-emerald-400" /></label></>}<div className="flex justify-end gap-3"><Button variant="secondary" type="button" onClick={() => setDialog(null)}>Cancel</Button><Button type="submit">Save changes</Button></div></form></Card></div>}</div>;
};
